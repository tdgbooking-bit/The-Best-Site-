{
  "name": "clothing-brand-website",
  "version": "1.0.0",
  "description": "A clothing brand website showcasing products and brand information.",
  "main": "src/index.html",
  "scripts": {
    "start": "live-server src",
    "build": "if not exist dist mkdir dist && xcopy src\\* dist /E /I /H /Y"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@openai/codex": "^0.98.0",
    "body-parser": "^2.2.2",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "openai": "^6.18.0"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
